import 'package:flutter/material.dart';

class ExploreReviewsScreen extends StatelessWidget {
  const ExploreReviewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}